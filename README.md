# Stargazer

Personal Digital Observatory — minimal starter scaffold.

## Setup

Install dependencies and run dev server:

```bash
npm install
npm run dev
```

## Notes
- Add API keys to `.env.local` (e.g. `NASA_API_KEY`, `N2YO_API_KEY`, `OPENWEATHER_API_KEY`).
- This scaffold includes a moving starfield, a Bortle slider, and a hooks skeleton for astronomy APIs.
